{
  "name": "signiflyscoreboard",
  "description": "Signifly tournament.",
  "author": "Reh Ernest",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "source": "src/index.html",
  "scripts": {
    "clean": "rimraf dist .parcel-cache",
    "toto": "rimraf app",
    "dev": "parcel --open",
    "stage": "parcel build --no-optimize --no-source-maps --dist-dir app --public-url '.'",
    "build": "parcel build --no-source-maps --dist-dir app --public-url '.'"
  },
  "packageManager": "yarn@3.0.2",
  "devDependencies": {
    "parcel": "^2.0.0-rc.0"
  },
  "dependencies": {
    "@parcel/transformer-sass": "^2.8.2",
    "rimraf": "^4.1.2"
  }
}
